# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# X (Twitter) API Configuration
# Get your bearer token from https://developer.twitter.com/
TWITTER_BEARER_TOKEN=your-twitter-bearer-token

# Translation API Configuration
# Configure based on your chosen translation service (Google Translate, DeepL, OpenAI, etc.)
TRANSLATION_API_KEY=your-translation-api-key

# Admin API Configuration (Optional)
# If set, the sync API endpoint will require this key in the X-API-Key header
# When not set, the sync endpoint will work without authentication
# Note: For production, implement proper authentication (e.g., NextAuth.js)
ADMIN_API_KEY=your-admin-api-key
